[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "bot_trading"
version = "0.1.0"
description = "Multi-Model Trading Research and Paper-Trading MVP System"
readme = "README.md"
requires-python = ">=3.10"
dependencies = []
